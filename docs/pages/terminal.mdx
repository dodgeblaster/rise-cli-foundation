# Terminal

## Introduction

Terminal represents utils for outputing text in the terminal

## filesystem.writeToTerminal

```ts
import utils from 'rise-cli-foundation'
await utils.writeToTerminal('my text')
```

## filesystem.clearTerminal

```ts
import utils from 'rise-cli-foundation'
await utils.clearTerminal()
```
